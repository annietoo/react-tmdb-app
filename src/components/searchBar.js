import React from 'react';

const SearchBar = (props) => {
	return (
		<div className="col-sm-4">
        	<input className="form-control searchBar" type="text" placeholder="Search"/>
        </div>
	);
}

export default SearchBar;